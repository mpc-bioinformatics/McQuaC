{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/nf-core/macproqc/master/assets/schema_input.json",
    "title": "nf-core/macproqc pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "sample": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Sample name must be provided and cannot contain spaces",
                "meta": ["id"]
            },
            "spectrum_file": {
                "type": "string",
                "format": "path",
                "exists": true,
                "pattern": "^\\S+\\.(raw|RAW|mzML|mzml|d|)(.tar.(gz|bz2)|.zip|.gz)?$",
                "errorMessage": "MS file name, cannot contain spaces and must have one of the extensions: raw | RAW | mzML | d (or compressed version)"
            }
        },
        "required": ["sample", "spectrum_file"]
    }
}
