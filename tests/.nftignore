.DS_Store
pipeline_info/*.{html,json,txt,yml}

# XML files are tested separately via the spectrum_counts snapshot assertion
mzmls/*.mzML
comet/*.mzid

# PIA results are not not exactly equal (not sorted) -> test separately
pia/*.{mzTab,csv}
