// TODO nf-core: Once you have added the required tests, please run the following command to build this file:
// nf-core modules test qcvisualization
nextflow_process {

    name "Test Process QCVISUALIZATION"
    script "../main.nf"
    process "QCVISUALIZATION"

    tag "modules"
    tag "modules_"
    tag "qcvisualization"

    test("qcvisualization - hdf5 - testdata") {

        // TODO nf-core: If you are created a test for a chained module
        // (the module requires running more than one process to generate the required output)
        // add the 'setup' method here.
        // You can find more information about how to use a 'setup' method in the nf-test docs (https://www.nf-test.com/docs/testcases/setup/).

        when {
            process {
                """
                input[0] = [
                    [ id:'test' ],
                    file('/mnt/data/Documents/McQuaC/testfiles/EXII01980std.hdf5', checkIfExists: true)
                ]
                input[1] = 'testfiles'   // output folder
                """
            }
        }

        then {
            assert process.success
            assertAll(
                { assert snapshot(
                    process.out,
                    process.out.findAll { key, val -> key.startsWith('versions') }
                ).match() }
                //TODO nf-core: Add all required assertions to verify the test output.
                // See https://nf-co.re/docs/developing/testing/assertions for more information and examples.
            )
        }

    }

}
